server:
  port: 8085 #记得修改端口号，防止冲突
feign:
  okhttp:
    enabled: true
  sentinel:
    enabled: true

hm:
  swagger:
    title: 交易服务接口文档
    package: com.hmall.trade.controller
  db:
    database: hm-trade